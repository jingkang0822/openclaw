---
summary: "CLI reference for `clawx sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `clawx sessions`

List stored conversation sessions.

```bash
clawx sessions
clawx sessions --active 120
clawx sessions --json
```
